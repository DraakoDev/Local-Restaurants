import React from 'react'
import { useParams } from 'react-router-dom'

const RestaurantInfo = () => {
  const { id } = useParams()

  return (
    <div className='h-screen w-full bg-black-600'>
      <div className=''></div>

    </div>
  )
}

export default RestaurantInfo